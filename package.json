{
  "name": "graph-adventures-mcp",
  "version": "0.2.0",
  "description": "Adventure-agnostic MCP engine (Azure Functions v4, Node.js) for gamebook-style adventure knowledge graphs — dungeon crawl, spy mystery, unseelie fae, dragon-hoard audit, all on one graph layout and one set of walkers",
  "main": "src/functions/*.js",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "func start",
    "web": "node scripts/serve-web.js",
    "test": "node --test test/*.test.js",
    "validate": "node scripts/validate-adventure.js",
    "check:reachability": "node scripts/check-reachability.js",
    "simulate": "node scripts/simulate-playthroughs.js",
    "smoke:entrypoint": "node scripts/verify-functions-entrypoint.js"
  },
  "dependencies": {
    "@azure/functions": "^4.7.0",
    "@azure/storage-blob": "^12.27.0"
  }
}
